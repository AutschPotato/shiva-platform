# Dummy Service

Lightweight local target service for the platform-side test stack.

It is intentionally technical rather than business-realistic and is used for:

- simple success-path traffic
- payload transport tests
- auth token scenarios
- explicit `3xx`, `4xx`, `5xx`, and timeout endpoint behavior

This directory is intended to stay with the future platform repository.
